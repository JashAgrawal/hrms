// Project components barrel exports

// Note: Project components will be created as needed
// For now, we'll export the ProjectSelect from timesheets since it's project-related
export { ProjectSelect } from '../timesheets/project-select'
